[project]
name = "app"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
start = "app.main:start"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
